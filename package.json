{
  "name": "drop-local",
  "version": "0.7.0",
  "description": "Local-first file sharing for teams with chunked uploads and group delivery.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": { "node": ">=22.5" }
}
