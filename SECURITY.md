# Security Policy

Drop در نسخهٔ فعلی برای شبکهٔ داخلی مورد اعتماد طراحی شده است. Port Forward مستقیم از مودم به برنامه پشتیبانی نمی‌شود.

پیش از دسترسی اینترنتی، HTTPS، احراز هویت قوی یا دومرحله‌ای، Rate limiting، CSRF protection، مدیریت نشست‌ها، اعتبارسنجی Proxy headers، اسکن فایل و Backup منظم ضروری است.

آسیب‌پذیری‌ها را همراه با دادهٔ واقعی کاربران در Issue عمومی منتشر نکنید؛ از Private vulnerability reporting مخزن استفاده کنید.
